# Oksigenia Checker MCP — configuration
#
# The engine is self-contained: it performs its own DNS-over-HTTPS, HTTP, RDAP
# and TLS lookups. There is no API key or backend to configure.

# Default report language: es | en   (default: en)
# OKSIGENIA_LANG=es
